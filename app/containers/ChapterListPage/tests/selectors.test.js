// import { selectChapterListPageDomain } from '../selectors';

describe('selectChapterListPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
