// import { selectExamListPageDomain } from '../selectors';

describe('selectExamListPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
